services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: devpassword
      POSTGRES_DB: playops
    ports:
      - "5432:5432"
    volumes:
      - playops_pgdata:/var/lib/postgresql/data

volumes:
  playops_pgdata:
